// interface Persons {
//     name : string;
//     age : number;
// }

type Persons = {
    name : string;
    age:number;
}

var seho: Persons = {
    name : '세호',
    age:30
}

type MyString = string;
var strrr:MyString = 'hello';

type Tode = {id : string; title:string; done:boolean};
function getTode(todo:Tode) {
    
}
